import { Box } from '@mui/material';
import React from 'react';

const Feed = () => {
  return (
    <Box bgcolor="purple" p={3} flex={4} sx={{ display: { sm: 'block' } }}>
      Feed
    </Box>
  );
};

export default Feed;
